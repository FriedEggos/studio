
{
  "entities": {
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user of the system, either a student or an admin.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user entity, matches Firebase Auth UID."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "Display name of the user."
        },
        "role": {
          "type": "string",
          "description": "Role of the user (e.g., student, admin).",
          "enum": ["student", "admin"]
        },
        "createdAt": {
            "type": "string",
            "format": "date-time",
            "description": "Timestamp when the user account was created."
        }
      },
      "required": [
        "id",
        "email",
        "displayName",
        "role",
        "createdAt"
      ]
    },
    "Program": {
      "title": "Program",
      "type": "object",
      "description": "Represents a program or event with a scannable QR code for attendance.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the program entity."
        },
        "title": {
          "type": "string",
          "description": "Title of the program."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the program."
        },
        "location": {
          "type": "string",
          "description": "Location where the program will be held."
        },
        "startDate": {
          "type": "string",
          "description": "Start date and time of the program.",
          "format": "date-time"
        },
        "endDate": {
          "type": "string",
          "description": "End date and time of the program.",
          "format": "date-time"
        },
        "status": {
            "type": "string",
            "description": "Current status of the program.",
            "enum": ["upcoming", "ongoing", "completed"]
        },
        "qrSlug": {
            "type": "string",
            "description": "A unique, short, URL-friendly slug for the public QR form link."
        },
        "redirectUrl": {
            "type": "string",
            "description": "An optional URL to redirect the user to after they submit the attendance form."
        },
        "createdBy": {
          "type": "string",
          "description": "Reference to the User (admin) who created the program."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the program was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "title",
        "description",
        "startDate",
        "endDate",
        "status",
        "qrSlug",
        "createdBy",
        "createdAt"
      ]
    },
    "ProgramConfig": {
        "title": "Program Configuration",
        "type": "object",
        "description": "Defines the settings and fields for a program's public attendance form.",
        "properties": {
            "copywriting": {
                "type": "string",
                "description": "Custom text or instructions to display at the top of the attendance form."
            },
            "fields": {
                "type": "object",
                "properties": {
                    "requireStudentId": { "type": "boolean" },
                    "requirePhone": { "type": "boolean" },
                    "requireEmail": { "type": "boolean" },
                    "requireClass": { "type": "boolean" },
                    "customInput1Enabled": { "type": "boolean" },
                    "customInput1Label": { "type": "string" },
                    "customInput2Enabled": { "type": "boolean" },
                    "customInput2Label": { "type": "string" }
                }
            }
        }
    },
    "Attendance": {
      "title": "Attendance",
      "type": "object",
      "description": "Represents a single attendance submission from the public QR form.",
      "properties": {
        "programId": {
          "type": "string",
          "description": "Reference to the Program."
        },
        "studentName": {
            "type": "string",
            "description": "Full name of the attending student."
        },
        "studentId": {
            "type": "string",
            "description": "Matriculation ID of the student."
        },
        "phone": {
            "type": "string",
            "description": "Phone number of the student."
        },
        "email": {
            "type": "string",
            "description": "Email address of the student."
        },
        "classGroup": {
            "type": "string",
            "description": "The student's class or group."
        },
        "customInput1": {
            "type": "string",
            "description": "Value for the first custom field."
        },
        "customInput2": {
            "type": "string",
            "description": "Value for the second custom field."
        },
        "createdAt": {
          "type": "string",
          "description": "The timestamp when the attendance was recorded.",
          "format": "date-time"
        },
        "userAgent": {
            "type": "string",
            "description": "The browser/device user agent of the person submitting the form."
        }
      },
      "required": ["programId", "studentName", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/entities/User"
          },
          "description": "Stores individual user profile data (students and admins)."
        }
      },
      {
        "path": "/programs/{programId}",
        "definition": {
          "entityName": "Program",
          "schema": {
            "$ref": "#/entities/Program"
          },
          "description": "Stores details for all programs and events."
        }
      },
      {
        "path": "/programConfigs/{programId}",
        "definition": {
            "entityName": "ProgramConfig",
            "schema": {
                "$ref": "#/entities/ProgramConfig"
            },
            "description": "Stores the form configuration for a specific program."
        }
      },
      {
        "path": "/programs/{programId}/attendances/{attendanceId}",
        "definition": {
            "entityName": "Attendance",
            "schema": {
                "$ref": "#/entities/Attendance"
            },
            "description": "Stores individual attendance submissions for a program."
        }
      }
    ],
    "reasoning": "This Firestore structure separates core program data from its form configuration and its attendance records. Public data (`programs`, `programConfigs`) is structured to be readable by unauthenticated users on a per-document basis for the QR form page, while sensitive data like attendance lists is nested and protected. This provides a secure and scalable way to handle public attendance submissions while giving administrators full control and visibility."
  }
}
